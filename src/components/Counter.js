import { View, Text, Button} from 'react-native';

export default function Counter() {
    return (
        <View>
            <Text>you clicked the button o times</Text>
            <Button title='Click me'></Button>
        </View>
    )
}